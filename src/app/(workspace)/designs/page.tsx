import { SectionPlaceholder } from "@/components/application/section-placeholder";

export default function DesignsPage() {
  return (
    <SectionPlaceholder
      eyebrow="Design Library"
      title="Biblioteca visual"
      description="Reunirá las familias, arquetipos y variantes disponibles sin convertir Content Publisher en un editor gráfico libre."
      next="Traducir el catálogo documentado a definiciones versionadas y comenzar con los primeros arquetipos React."
    />
  );
}
