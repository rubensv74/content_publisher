import { SectionPlaceholder } from "@/components/application/section-placeholder";

export default function SettingsPage() {
  return (
    <SectionPlaceholder
      eyebrow="Identity"
      title="Identidad visual"
      description="Firma, tipografías, paletas, series y elementos recurrentes vivirán en una configuración central para que todas las publicaciones compartan ADN sin ser repetitivas."
      next="Convertir la dirección visual elegida en tokens de identidad editables y persistentes."
    />
  );
}
